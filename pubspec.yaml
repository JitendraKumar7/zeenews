name: zeenews
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.17.0
  http: ^0.13.4
  equatable: ^2.0.3
  flutter_bloc: ^8.0.0
  empty_widget: ^0.0.5
  flutter_html: ^2.2.1
  cupertino_icons: ^1.0.4
  cached_network_image: 3.2.0

  # Add plugin to flutter project
  taboola:
    path: packages/taboola

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.4

flutter:

  uses-material-design: true

  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg